name: HomeWork_8

on: [push]

jobs:


  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0

        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}




      - name: Join to server
        run: |
          ssh -v -o StrictHostKeyChecking=no wioswa@51.250.96.80