name: Django CI

on: [push]

jobs:


  deploy:
    runs-on: ubuntu-latest


    steps:


    - name: Copy project files to server
#      run: |
#        ssh ${{ secrets.SSH_USER }}@${{ secrets.SERVER_IP }}
#        ${{ secrets.CLONE_REPO }}
      run: ssh wioswa@158.160.92.99